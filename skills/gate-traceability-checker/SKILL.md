# 规范

本md文件，上下文指令必须 < 2k token，内部只能包含对代码、资源文件的链接，不允许深层嵌套。

资源文件已经拆分到res/scripts（代码块）与res/refs（模板与参考资料）中。

skills/skillnamefold 作为一个完整的知识包。


# 职责

强制约束Agent行为，执行追溯链条检查。

检查当前的，所处的状态阶段内的步骤是否全部正确完成（完成规则需要制定，因地制宜）。

若完成，则允许通过，进度下一阶段。

若未完成，阻止阶段切换，并提示缺少的步骤给出具体修复建议。



门禁阶段状态："需求描述 -> 设计方案 -> 代码实现-> 代码测试"。

状态阶段存在于：（特定项目的状态文件），如若完成则允许进行到需求的下一个阶段处理。

需求定义并初始化。

# 具体的检查机制（待补充，根据实际工作流程来）

需求描述（该阶段有啥门禁判断指标？）

设计方案（该阶段有啥门禁判断指标？）（从 requirement-bootstrapper 到 managing-feature-lifecycle的Skill执行完毕后，确定通过了设计方案的门禁，开始进入下一步：代码实现？）

代码实现（该阶段有啥门禁判断指标？）

单元测试（该阶段有啥门禁判断指标？）
