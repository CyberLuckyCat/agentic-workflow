占位用

此处需要补充skill知识包的资源引用。


如：

field-spec.md - 字段含义，类型，不同语言之间的映射方式。

enum-types.md - 枚举值大全（28种Inspire类型，48个Fact方法）

config-structure.md - 特定配置表格的完整结构

when-syntax.md - When条件的GRL字符串语法+转义规则

db-tables.md - 4张数据库表DDL+写入规则

athena-mcp-tools.md - 特定MCP的调用模板
