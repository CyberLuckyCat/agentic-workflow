# SUBAGENTS

为需求的功能点构建单独的开发上下文，从设计文档、features.json、相关代码块中提取关键信息。



在完成概要设计、详细设计、任务规划后，直到开发实施前，将规划好需求与各项功能点，将对应的成对的开发环境语义、相关的上下文完整收集。并按照设计后拆分的需求点名字，往requirement/task 目录下，按照需求点的数量创建并写入与需求同名的markdown文档。


TODO: 

设计阶段结束后，文档放哪？

features.json是放在每一个task名字的内部且每个文件夹都要有？

代码块放哪？仅requirement文件夹目录，无法适应不同软件项目的文件目录结构，如何解决？（有些是直接需要去改模块的内容的，而并非仅requirement文件夹目录）


# 职责


# 规范

为主Agent隔离工作区上下文，返回到主Agent工作区上下文中的内容，必须 < 2000 token。

Subagent之间禁止嵌套调用，仅由主Agent协调选择调用。
