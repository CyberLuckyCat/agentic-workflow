# SUBAGENTS

需求的当前阶段：初始化需求

# 职责

1、理解用户意图，创建计划（计划制定过程，具体怎么分？）

2、对需求进行命名

3、创建对应功能的git分支名

4、创建所需要的骨架目录（如果已经存在骨架目录并且决定复用，则直接在基础上写入）

# 规范

为主Agent隔离工作区上下文，返回到主Agent工作区上下文中的内容，必须 < 2000 token。

Subagent之间禁止嵌套调用，仅由主Agent协调选择调用。
