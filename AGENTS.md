# AGENTS

我是Unity游戏开发的资深工程师（Persona后续补全），类似于AI对话窗口的系统提示词。


RooCode的分布Roles操作，估计还是无法跟Claude Code结合Harness Engineering的这种知识沉淀、脚手架、马具（随便怎么叫）的效果相比。


逐步来吧，先RooCode摸索一下，能不能达到轻量的模块开发实现。

TODO: Claude Code这种CLI迟早要跑的，最终按照一个教程，自己写一个Claude Code。


# 职责

文档类的输出，严格按照步骤输出清单：

1、先输出3-5条摘要或决策点，如：已从上下文中获取的关键信息（"从 context/project/ 中找到了 X 服务的接口规范，将据此定义需求边界"），如：涉及选择的决策点（"有两种实现方案，各有利弊如下，请确认方向"）

2、等待用户确认信息，如：以下 3多条信息无法从项目上下文中确认，请提供来源或给出假设

3、开始撰写正式文档，输出完整详情。

subagents通过mcp调用其他工具的输出时，若工具输出过长则写入文件，用tail（skill？）按需读取（长响应转文件）。

mcp工具只加载名称，详细描述存文件夹按需检索。

技能描述作为静态上下文，详细内容动态发现。

这里原是放在context的同级根目录下，但：多agent概念还未明确时，无法归类，太零散。

单个Agent仅作为用户输入意图（需求）的索引，内容行数大约100行

仅靠描述信息的自动匹配，调用稳定性依旧不够

**需要强化"何时使用"和"如何使用"特定工具的指引，提高 Skill 和 Subagent 调用的稳定性和准确性。**

# **通过requirement识别意图，通过意图匹配工具后拉取工具进行调用**

调用时才加载领域知识（保证上下文干净，仅加载当前工具需要的知识）
